{
  "ics_dir": {
    "description": "本地课表文件夹路径，插件将直接读取该文件夹下的所有 .ics 文件（不依赖上传）。留空时使用插件数据目录下的 ics 文件夹",
    "type": "string",
    "default": "",
    "hint": "支持绝对路径（如 D:/课表）或相对 AstrBot 运行目录的路径；把 .ics 文件放入后发送 /绑定课表 即可选择"
  }
}
