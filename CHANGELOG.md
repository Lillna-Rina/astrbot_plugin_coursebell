# 更新日志

## v1.6.1 (2026-09-11)

### 修复

- **修复 `/设置倒计时`、`/设置调休` 等多词参数指令提示"格式不正确"的问题**：
  AstrBot 命令框架中，`args: str = ""` 形式的参数只会收到按空格拆分后的**第一个词**；
  改为 `args: GreedyStr`（注解不带默认值）后，剩余文本会完整传入，参数解析恢复正常
- 同样修正 `/删除倒计时`、`/删除调休` 的参数签名
- 新增框架参数传递契约回归测试（复现旧行为 + 验证新行为）

## v1.6.0 (2026-09-10)

### 新增：日期倒计时（考研倒计时等）

- `/设置倒计时 <名称> <日期> [每日|每周|关闭] [HH:MM]`：添加倒计时，支持**每日通知**、**每周通知**（每周一）或关闭通知
  - 示例：`/设置倒计时 考研 2026-12-26 每日 08:00`
- `/倒计时`：查看全部倒计时与剩余天数；`/删除倒计时 考研`：删除
- 倒计时通知持久化，插件重启后自动恢复；日课表图片顶部显示最近一个倒计时

### 新增：调休课表映射

- `/设置调休 09-27=10-07`：把 9月27日 调为 10月7日 的课表（9月27日按 10月7日 的课上课）
- `/设置调休 10-07=放假`：设为放假无课（也支持「休息」「无课」「off」）
- `/查看调休`、`/删除调休 09-27`、`/清空调休`
- 调休后**课表查询、周课表、每日推送、课前提醒**全部按调休后的课表执行：
  - 课程内容取自被映射的日期，**日期平移到当天、上课时刻不变**
  - 放假日不推送、不提醒
- 日课表图片显示「🔄 调休：按 X月X日 课表」或「🚫 放假」标签，周课表在对应日期标注

> 采用「单向映射 + 显式放假」而非双向交换：符合真实调休语义（补课日上课、放假日休息），
> 且避免 A↔B 循环映射带来的歧义；如需互换，分别设置两条规则即可。

### 其他

- 参数解析逻辑抽到 `command_parsers.py`（不依赖 astrbot 运行时，便于测试）
- 新增 40 项测试（倒计时天数、日期解析、映射、课表映射、提醒命中、参数解析）

## v1.5.1 (2026-09-07)

- **修复**：第 2 周及以后不显示新开课程的问题（如第 1 周 ABC、第 2 周应 ABCD 却只显示 ABC）——解析时自动识别课程文本中的周次范围（「2-16周」「4-5周,7-12周」「单周」「双周」），按校历周次过滤课程实例
- **新增**：插件配置 `term_start_date`（学期开始日期，格式 YYYY-MM-DD），作为校历第 1 周的基准；留空时自动以课表文件中最早课程的所在周作为第一周
- 周课表副标题显示「第N周」；`/查看设置` 显示学期开始日期与当前周次
- 无周次信息的课程保持原行为（每周显示），完全兼容旧课表

## v1.5.0 (2026-08-31)

- 重新支持上传 .ics 文件绑定：`/绑定课表` 会话中直接发送 .ics 文件即自动下载到课表文件夹并绑定（保存行为与手动放入文件夹一致）
- 上传文件使用原始文件名（冲突时自动加时间戳后缀）
- 课表图片改为**自适应大小的 3:4 比例**：内容少时 420×560、内容多时按内容自动放大画布（最大 600×800），比例始终严格 3:4、不丢课程、不丢可读性

## v1.4.0 (2026-08-31)

- 课表图片改为固定 3:4 比例输出（420x560 画布）
- 内容较少时铺满画布，内容较多时 JS 等比缩放，保证比例不变且课程信息完整
- 周课表改为 2 列紧凑网格布局，满课场景缩放系数约 0.8，保持良好可读性
- 真实浏览器验证：全部场景严格 3:4

## v1.3.0 (2026-08-31)

- 插件更名：**课铃（CourseBell）**，插件名由 `astrbot_plugin_course_reminder` 改为 `astrbot_plugin_coursebell`（GitHub 仓库同步更名）
- 自动迁移旧插件名数据目录中的绑定信息与课表文件，升级后无需重新绑定
- README 改为社区标准格式（徽章、功能概览、命令表格、项目结构、Star History），新增 LICENSE（MIT）与 CHANGELOG

## v1.2.0 (2026-08-31)

- 课表图片改为竖屏设计：今日/明日课表窄宽纵向卡片（420px），本周/下周课表由横向四列网格改为 7 天纵向列表
- 真实浏览器渲染验证：今日课表 840x1800、本周课表 840x2746（2x 缩放），适合手机查看
- 渲染选项保持 `full_page`，图片高度随课程数量自适应

## v1.1.1 (2026-08-31)

- 修复示例课表功能：生成与写入分离错误处理，失败时给出具体原因
- 课表文件夹不可写时自动回退到插件数据目录
- 配置的 `ics_dir` 无法创建时回退默认目录并在日志与提示中记录原因
- 用户 ID 用于文件名时做安全字符过滤（含 `:`、`/` 等平台 ID）
- 绑定记录支持绝对路径（回退写入场景）

## v1.1.0 (2026-08-31)

- 重大改动：课表来源改为**直接读取本地文件夹**，不再依赖用户上传
- 新增插件配置 `ics_dir`（本地课表文件夹路径）
- `/绑定课表 [文件名]` 选择本地文件绑定；文件名以用户 ID 开头自动匹配
- 新增 `/课表文件` 查看文件夹文件列表
- 文件变更自动检测（mtime+size），替换文件即生效
- `/删除课表` 仅解除绑定，不再删除本地文件
- 新增 `_conf_schema.json` 配置 Schema

## v1.0.0 (2026-08-31)

- 初始版本：绑定 .ics 课表，查看今日/明日/本周/下周课表
- 课前提醒（每 60 秒扫描，提前分钟数可配置）
- 每日定时推送课表（cron 持久化）
- 图片渲染失败自动降级为文本
